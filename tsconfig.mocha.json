{
  // TypeScript configuration for mocha cross-platform support only. Not used for build.
  "extends": "./tsconfig.json",
  "exclude": [
    "**/node_modules",
    "**/tmp",
    "**/dist",
    "**/coverage",
    "**/test",
    "**/tests",
    "**/__tests__",
    "**/__mocks__",
    // Ignore non-ESM files
    "lib/highlight.ts"
  ]
}
