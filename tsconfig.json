{
  // TypeScript configuration for IDE support only. Not used for build.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "tmp/dist",
    "noEmit": true,
    "types": ["node", "mocha"]
  },
  "exclude": [
    "**/node_modules",
    "**/tmp",
    "**/dist",
    "**/coverage",
    "**/test",
    "**/tests",
    "**/__tests__",
    "**/__mocks__"
  ]
}
